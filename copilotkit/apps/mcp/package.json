{
  "name": "mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The same capabilities as an MCP server — the ChatGPT, Claude and Codex surface.",
  "scripts": {
    "dev": "node --env-file=../../.env --import tsx --watch src/server.ts",
    "start": "node --env-file=../../.env --import tsx src/server.ts",
    "stdio": "node --env-file=../../.env --import tsx src/stdio.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "agent-core": "*",
    "zod": "^4.1.0"
  }
}
